import java.util.Iterator;

public class ZooIterator<Animal>  implements Iterator<Animal> {
    public boolean hasNext(){
        while(iterator)
    }
    public Animal next(){
        return Animal;
    }
}
